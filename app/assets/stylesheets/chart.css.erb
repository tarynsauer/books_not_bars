/*! HTML5 Boilerplate v4.3.0 | MIT License | http://h5bp.com/ */

/*
 * What follows is the result of much research on cross-browser styling.
 * Credit left inline and big thanks to Nicolas Gallagher, Jonathan Neal,
 * Kroc Camen, and the H5BP dev community and team.
 */

/* ==========================================================================
   Base styles: opinionated defaults
   ========================================================================== */

html,
button,
input,
select,
textarea {
    color: #222;
    overflow-x: hidden;
}

html {
    font-size: 1em;
    line-height: 1.4;
}

/*
 * Remove text-shadow in selection highlight: h5bp.com/i
 * These selection rule sets have to be separate.
 * Customize the background color to match your design.
 */

::-moz-selection {
    background: #b3d4fc;
    text-shadow: none;
}

::selection {
    background: #b3d4fc;
    text-shadow: none;
}

/*
 * A better looking default horizontal rule
 */

hr {
    display: block;
    height: 1px;
    border: 0;
    border-top: 1px solid #ccc;
    margin: 1em 0;
    padding: 0;
}

/*
 * Remove the gap between images, videos, audio and canvas and the bottom of
 * their containers: h5bp.com/i/440
 */

audio,
canvas,
img,
video {
    vertical-align: middle;
}

/*
 * Remove default fieldset styles.
 */

fieldset {
    border: 0;
    margin: 0;
    padding: 0;
}

/*
 * Allow only vertical resizing of textareas.
 */

textarea {
    resize: vertical;
}

/* ==========================================================================
   Browse Happy prompt
   ========================================================================== */
html,
body{
  height: 100%; width: 100%;
  margin: 0; padding: 0;
  min-height: 2000px;
}
.browsehappy {
    margin: 0.2em 0;
    background: #ccc;
    color: #000;
    padding: 0.2em 0;
}

nav{
  position: fixed;
  top:15%;
  left:5%;
  z-index:2;
  width: 15%;
}

nav ul {
  padding: 0;
}
nav ul li{
  list-style: none;
  padding: 10px 0px;
}

nav > ul > li > a {
  text-decoration: none;
  font-weight: 300;
  color: white;
  font-size: auto;
}

.black {
  color: black;
}

.white {
  color: white;
}

a.black:link {color:'#000'; font-weight: 300;}
a.black:visited {color:'#000'; font-weight: 300;}
a.black:hover {color:'#000'; font-weight: 700;}
a.black:active {color:'#000'; font-weight: 700;}

a.white:link {color:'white';font-weight: 300;}
a.white:visited {color:'white';font-weight: 300;}
a.white:hover {color:'white';font-weight: 700;}
a.white:active {color:'white'; font-weight: 700;}

.cover_gradient{
position:absolute;
top:0;
background: -moz-radial-gradient(center, ellipse cover,  rgba(0,0,0,0) 0%, rgba(0,0,0,0.38) 52%, rgba(0,0,0,0.94) 100%); /* FF3.6+ */
background: -webkit-gradient(radial, center center, 0px, center center, 100%, color-stop(0%,rgba(0,0,0,0)), color-stop(52%,rgba(0,0,0,0.38)), color-stop(100%,rgba(0,0,0,0.94))); /* Chrome,Safari4+ */
background: -webkit-radial-gradient(center, ellipse cover,  rgba(0,0,0,0) 0%,rgba(0,0,0,0.38) 52%,rgba(0,0,0,0.94) 100%); /* Chrome10+,Safari5.1+ */
background: -o-radial-gradient(center, ellipse cover,  rgba(0,0,0,0) 0%,rgba(0,0,0,0.38) 52%,rgba(0,0,0,0.94) 100%); /* Opera 12+ */
background: -ms-radial-gradient(center, ellipse cover,  rgba(0,0,0,0) 0%,rgba(0,0,0,0.38) 52%,rgba(0,0,0,0.94) 100%); /* IE10+ */
background: radial-gradient(ellipse at center,  rgba(0,0,0,0) 0%,rgba(0,0,0,0.38) 52%,rgba(0,0,0,0.94) 100%); /* W3C */
filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#00000000', endColorstr='#f0000000',GradientType=1 ); /* IE6-9 fallback on horizontal gradient */
  z-index:-1;
  height: 800px;
  width: 100%;

}
header{
  height:57px;
  background: rgba(21,21,21,1);
  width: 100%;
  position: fixed;
  top:0; left: 0;
  z-index:40;
}

.cover{
  background: url('blackorchid.png') repeat;
  position: absolute;
  top: 0;
  height: 100%; width: 100%;
  opacity: 0.7;
}

.center{
  display: table;
  margin: 0 auto;
  text-align: center;
}

section{
  height:950px;
  width: 100%;
  top: 0; left:0;
  -webkit-perspective: 15000; perspective: 15000;
}
section#facts{
background-color: black;
color: white;
}

section#explore {
  background-color: rgba(218,33,50,1);
}

section#toolkit{
background-color: white;
/*height: 100%;*/
}

section#situation{
  background: url(edubg1.jpg) no-repeat center center;
  -webkit-background-size: cover;
  -moz-background-size: cover;
  -o-background-size: cover;
  background-size: cover;
}
section#situation > div.intro{
  position: relative;
  top: 280px;
  width: 600px;
  height: 500px;
  color: rgba(254,254,254,0.9);
  font-family: 'Dist', sans-serif;
  font-weight: 100;
}
section#situation > div.intro h1.main{
  font-family: 'FuturaNDBook';
  font-size: 80px;
  margin: 0;
  text-shadow: 0 0 2px rgba(255,255,255,1), 0 0 2px rgba(255,255,255,1), 0 0 2px rgba(255,255,255,1), 0 0 3px rgba(218,33,50,0.7), 0 0 3px rgba(218,33,50,0.7), 0 0 3px rgba(218,33,50,0.7), 0 0 3px rgba(218,33,50,0.7), 0 0 3px rgba(218,33,50,0.7);
}

section#situation button{
  border: none;
  border-radius: 8px;
  background-color: rgba(218,33,50,0.9);
  width: 250px;
  height: 66px;
  margin:10px;
  color: rgba(254,254,254,0.9);
}

section#explore{
  position: relative;

}
section#explore > div.about{
  position: absolute;
  top:300px;
  left: 350px;
  width: 450px;
  height: auto;
  color: rgba(254,254,254,0.9);
  font-family: 'Dist', sans-serif;
  font-weight: 100;
}
section#explore >  h1{
  color: rgba(255,255,255,1);
  font-family: 'FuturaNDBook';
  font-size: 60px;
  margin: 0px;
  position: relative;
  top:80px;
  left: 350px;
  text-shadow: 0 0 2px rgba(255,255,255,1), 0 0 2px rgba(255,255,255,1), 0 0 2px rgba(255,255,255,1), 0 0 3px rgba(21,21,21,0.7), 0 0 3px rgba(21,21,21,0.7), 0 0 3px rgba(21,21,21,0.7), 0 0 3px rgba(21,21,21,0.7), 0 0 3px rgba(21,21,21,0.7);
}
section#explore > div.about p{
  font-size: 18px;
}

/* ==========================================================================
   Map's custom styles
   ========================================================================== */
/*section#explore,
.map_container{
  width: 100%;
  height: 100%;
  position: fixed;
  top:0;
  left: 0;
  z-index:1;
}
*/
section#explore {
  background: rgba(218,33,50,1);
/*  margin-top: 0px;*/
-webkit-perspective: 15000; perspective: 15000;
}

section#explore > div#vmap{
  -webkit-transform-origin: bottom center;
  -webkit-transform-style: preserve-3d;
  -webkit-transition: all 1s ease-in-out;
  margin: 0 auto;
  height: 55%; width: 55%;
  opacity: 0.9;
  top: 20%;
  left:15%;
}

section#explore > div#vmap > svg > g > path{
  -webkit-filter: drop-shadow(0px 10px 0px rgba(255,255,255,1),
    0px 14px 10px rgba(255,255,255,1),
    0px 24px 2px rgba(255,255,255,1),
    0px 34px 30px rgba(255,255,255,1));
}

section#explore > div#vmap.display{
  -webkit-transform: translateY(50px) translateZ(200px) rotateX(65deg);
    display: table;
    text-align: center;
}


section#explore > div.stats{
  text-align: right;
  color: rgba(240,242,241,0.8);
  background: rgba(21,21,21,1);
  position: absolute;
  width: 230px;
  height: 100%;
  top: 0;
  right:0;
  padding:0 0 0 0; margin: 0;
}
section#explore > div.stats > ul {
  padding:80px 0 0; margin: 0;
  text-align: right;
  font-size: 14px;
  height: 300px;
}
section#explore > div.stats ul > li{
  list-style: none;
  padding:5px 0;
  border-bottom: #ccc inset 1px;
  border-top: #ccc inset 1px;
  text-align: right;
  width: 100%;
}

section#explore > div.stats ul > li:last-child{
  /*border: none;*/
  /*padding-top: 80px;*/
}


section#explore > div.stats ul > li > h3,
section#explore > div.stats ul > li > p{
  display: inline;
  margin: 4px 18px;
}

section#explore > div.stats > ul > div#chart_holder{

  padding:10px;
}

section#explore > div.stats > ul > div#chart_holder{
  height: ;
}



section#explore div#donut_holder{
  position: relative;
  bottom: 0px;
  left:0;
  opacity: 1;
  border: 1px blue solid;
  -webkit-transition: all 1s ease-in-out;
}


section#explore > div.stats > div#donut_holder .arc,
section#explore > div.stats > div#donut_holder g{
  -webkit-transition: all 1s ease-in-out;
}

section#explore > div#vmap > .arc_show{
  opacity: 0.6;
  height:950px;
  background-color: white
  color: black;
}


button#close{
  border: none;
  border-radius: none;
  width: 200px;
  height: 50px;
  background: rgba(21,21,21,1);
  position: absolute;
  bottom:10%; left:350px;
  z-index:999;
  color:rgba(240,242,241,0.95);
}

section#facts{
  background: url(sadInPrisonBW.jpeg) no-repeat center center;
  -webkit-background-size: cover;
  -moz-background-size: cover;
  -o-background-size: cover;
  background-size: cover;
}
section#facts > div.intro{
  position: relative;
  top: 280px;
  width: 600px;
  height: 500px;
  color: rgba(254,254,254,0.9);
  font-family: 'Dist', sans-serif;
  font-weight: 100;
}
.factPanel {
  top: 32%;
  margin-left: 30%;
  margin-right: 20%;
  position: absolute;
}

.factPanel div {
  border: 0px solid white;
  display: inline-block;
  height: 500px;
  width: 880px;
  margin: 20px;
}

.fact-red{
  color: rgba(218,33,50,1);
  font-size: 5em;
  border-top: 1px solid white;
  border-bottom: 1px solid white;

}

.fact-red2{
  color: rgba(218,33,50,1);
  font-size: 5em;
  /*border-top: 1px solid white;
  border-bottom: 1px solid white;
*/
}

.bigtext{
  font-size: 2em;
}
.fact-blue{
  color: rgba(218,33,50,1);
  font-size: 5em;

}
.icons{
  background-image: url(<%= asset_path 'circle-dashed-6-xxl.png' %>);
  background-repeat: no-repeat;
  background-size: cover;
  height: 10px;
  width: 10px;
}

.testMain {
  width:350px;
  border:solid 1px;
  color: white;
  border:solid 1px;
}



/* ==========================================================================
   SVG Styles
   ========================================================================== */

.chart text {
    fill: rgba(218,33,50,1);
}


.chart rect {
  stroke: none;
  fill: rgba(210,210,210,0.9);
}


.donut path,
.axis path,
.axis line {
  fill: none;
  /*stroke: #000;*/
  shape-rendering: crispEdges;
}

.y.axis line {
  stroke: #777;
  stroke-dasharray: 2,2;
}
.arrow {
  stroke: #000;
  stroke-width: 1.5px;
}


.outer,
.inner {
  shape-rendering: crispEdges;
}

.outer {
  fill: none;
  stroke: #000;
}

.inner {
  fill: #ccc;
  stroke: #000;
  stroke-dasharray: 3, 4;
}

.arc {
  fill: #ccc;
  stroke: #000;
  stroke-dasharray: 3, 4;
}

.arc text{fill: rgba(250,250,250,1);}

  /*fill: #ccc;*/
  /*stroke: #000;*/
  /*stroke-dasharray: 3, 4;*/
/*}*/

.arc path {
  shape-rendering: auto;
}


.axis text {
  font: 10px sans-serif;
}

.axis line,
.axis path {
  fill: none;
  stroke: #000;
  shape-rendering: crispEdges;
}


/* ==========================================================================
   Helper classes
   ==========================================================================

/*
 * Image replacement
 */

.ir {
    background-color: transparent;
    border: 0;
    overflow: hidden;
    /* IE 6/7 fallback */
    *text-indent: -9999px;
}

.ir:before {
    content: "";
    display: block;
    width: 0;
    height: 150%;
}

/*
 * Hide from both screenreaders and browsers: h5bp.com/u
 */

.hidden {
    display: none !important;
    visibility: hidden;
}

/*
 * Hide only visually, but have it available for screenreaders: h5bp.com/v
 */

.visuallyhidden {
    border: 0;
    clip: rect(0 0 0 0);
    height: 1px;
    margin: -1px;
    overflow: hidden;
    padding: 0;
    position: absolute;
    width: 1px;
}

/*
 * Extends the .visuallyhidden class to allow the element to be focusable
 * when navigated to via the keyboard: h5bp.com/p
 */

.visuallyhidden.focusable:active,
.visuallyhidden.focusable:focus {
    clip: auto;
    height: auto;
    margin: 0;
    overflow: visible;
    position: static;
    width: auto;
}

/*
 * Hide visually and from screenreaders, but maintain layout
 */

.invisible {
    visibility: hidden;
}

/*
 * Clearfix: contain floats
 *
 * For modern browsers
 * 1. The space content is one way to avoid an Opera bug when the
 *    `contenteditable` attribute is included anywhere else in the document.
 *    Otherwise it causes space to appear at the top and bottom of elements
 *    that receive the `clearfix` class.
 * 2. The use of `table` rather than `block` is only necessary if using
 *    `:before` to contain the top-margins of child elements.
 */

.clearfix:before,
.clearfix:after {
    content: " "; /* 1 */
    display: table; /* 2 */
}

.clearfix:after {
    clear: both;
}

/*
 * For IE 6/7 only
 * Include this rule to trigger hasLayout and contain floats.
 */

.clearfix {
    *zoom: 1;
}

/* ==========================================================================
   EXAMPLE Media Queries for Responsive Design.
   These examples override the primary ('mobile first') styles.
   Modify as content requires.
   ========================================================================== */

@media only screen and (min-width: 35em) {
    /* Style adjustments for viewports that meet the condition */
}

@media print,
       (-o-min-device-pixel-ratio: 5/4),
       (-webkit-min-device-pixel-ratio: 1.25),
       (min-resolution: 120dpi) {
    /* Style adjustments for high resolution devices */
}

/* ==========================================================================
   Print styles.
   Inlined to avoid required HTTP connection: h5bp.com/r
   ========================================================================== */

@media print {
    * {
        background: transparent !important;
        color: #000 !important; /* Black prints faster: h5bp.com/s */
        box-shadow: none !important;
        text-shadow: none !important;
    }

    a,
    a:visited {
        text-decoration: underline;
    }

    a[href]:after {
        content: " (" attr(href) ")";
    }

    abbr[title]:after {
        content: " (" attr(title) ")";
    }

    /*
     * Don't show links for images, or javascript/internal links
     */

    .ir a:after,
    a[href^="javascript:"]:after,
    a[href^="#"]:after {
        content: "";
    }

    pre,
    blockquote {
        border: 1px solid #999;
        page-break-inside: avoid;
    }

    thead {
        display: table-header-group; /* h5bp.com/t */
    }

    tr,
    img {
        page-break-inside: avoid;
    }

    img {
        max-width: 100% !important;
    }

    @page {
        margin: 0.5cm;
    }

    p,
    h2,
    h3 {
        orphans: 3;
        widows: 3;
    }

    h2,
    h3 {
        page-break-after: avoid;
    }
}
